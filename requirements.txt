# LLM (Groq - free)
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-groq>=0.2,<0.4
langchain-community>=0.3,<0.4
langchain-huggingface>=0.1,<0.4

# Vector DB
chromadb>=0.5,<1.0
langchain-chroma>=0.1,<0.3

# Embeddings (local, free)
sentence-transformers>=3.0,<4

# Agent framework (Phase 2)
langgraph>=0.2,<0.3

# DevOps integrations
kubernetes>=30,<31
prometheus-api-client==0.5.5

# API & UI
fastapi>=0.112,<1
uvicorn>=0.30,<1
python-dotenv>=1.0,<2

# Evaluation (Phase 3, optional; not required to run the API)
# ragas==0.1.14
